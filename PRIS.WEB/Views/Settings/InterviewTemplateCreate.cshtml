@*@model PRIS.WEB.ViewModels.InterviewTemplateViewModel.AddInterviewTemplateViewModel

@{
    ViewData["Title"] = "InterviewTemplateCreate";
    ViewData["ActivePage"] = PRIS.WEB.Views.Shared.ManageCollumnNavPages.InterviewTemplateList;
    Layout = "~/Views/Shared/_LayoutNavSectionForSettings.cshtml";
}*@

@*<form asp-action="InterviewTemplateCreate" method="post" class="form-horizontal" role="form">*@
@*<form asp-action="InterviewTemplateCreate">

    <div asp-validation-summary="All" class="text-danger"></div>

    <fieldset class="form-group">
        <label asp-for="AcademicYearID">Pasirenkamas mokslo metų laikotarpis</label>
        <select asp-for="AcademicYearID" asp-items="Model.AcademicYears" class="form-control"></select>
        <span asp-validation-for="AcademicYearID" class="text-danger"></span>
    </fieldset>

    <fieldset class="form-group">
        <label asp-for="SelectedInterviewTasks"></label>
        <br />
        @for (int i = 0; i < Model.SelectedInterviewTasks.Length; i++)
        {
            <label>@(i + 1)</label>
            <select asp-for="@Model.SelectedInterviewTasks[i]" asp-items="@Model.InterviewTasks" class="form-control"></select>
        }
        <span asp-validation-for="SelectedInterviewTasks" class="text-danger"></span>
    </fieldset>*@

    @*<input type="submit" value="Išsaugoti" class="btn btn-primary" />*@
    @*@if (Model.InterviewTemplateID > 0)
    {
        <input id="submitApplication" type="submit" value="Redaguoti formą" class="btn btn-outline-dark" />
    }
    else
    {
        <input id="submitApplication" type="submit" value="Sukurti naują formą" class="btn btn-outline-dark" />
    }
</form>*@
